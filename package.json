{
  "name": "voice-agent-scaffold",
  "private": true,
  "scripts": {
    "cleanup": "node scripts/cleanup-ports.js",
    "dev": "npm run cleanup && concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev -- --host 0.0.0.0\"",
    "backend": "npm run cleanup && npm --prefix backend run dev",
    "frontend": "npm run cleanup && npm --prefix frontend run dev -- --host 0.0.0.0",
    "start": "npm run cleanup && npm --prefix backend run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
